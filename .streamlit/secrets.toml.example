# Copy this file to .streamlit/secrets.toml and put the REAL key there.
# secrets.toml is gitignored - it will never be committed.
# On Streamlit Community Cloud, paste this same line into the app's
# Settings -> Secrets box instead (no file needed there).

OPENAI_API_KEY = "sk-put-the-real-key-here"
